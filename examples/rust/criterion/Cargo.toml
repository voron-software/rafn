[package]
name = "criterion-example"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
name = "criterion_example"
path = "src/lib.rs"

[[bench]]
name = "sample_bench"
harness = false

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
